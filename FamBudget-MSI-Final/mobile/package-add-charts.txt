Add these dependencies to mobile/package.json if not already present:

"dependencies": {
  ...existing dependencies...
  "react-native-chart-kit": "^6.12.0",
  "@react-native-community/slider": "^4.4.3"
}

Then run:
cd mobile
npm install

Note: These are needed for the Budget sliders and Reports charts.

